import type { Theme } from '@unocss/preset-uno'

export default {
  colors: {
    context: 'rgba(var(--u-c-context), %alpha)',
    primary: '#409EFF',
    success: '#67C23A',
    warning: '#E6A23C',
    danger: '#F56C6C',
    info: '#909399'
  }
} as Theme
